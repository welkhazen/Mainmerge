# Domain Docs

Layout: single-context.

- Primary domain glossary/context: `CONTEXT.md` at repo root when present.
- ADR directory: `docs/adr/` when present.

If missing, skills should infer terms from code and keep naming consistent with existing modules.
